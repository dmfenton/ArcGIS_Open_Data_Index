version: '2'
services:
  elasticsearch:
    image: elasticsearch:latest
  logstash:
    image: logstash:latest
    entrypoint: /tmp/logstashEntry.sh
    volumes:
      - ./:/tmp
    depends_on:
     - elasticsearch
    links:
      - elasticsearch
  kibana:
    image: kibana:latest
    ports:
      - "5601:5601"
    links:
      - elasticsearch
